import axios from 'axios';
import React, { useEffect } from 'react';

const useAddproduct = (addproduct) => {
console.log(addproduct);
  useEffect(()=>{
    // const url = `http://localhost:5000/product`;
    // axios.post(url,addproduct)
  },[])




  return [];
};

export default useAddproduct;